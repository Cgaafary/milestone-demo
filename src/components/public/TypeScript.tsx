import * as React from 'react';

const TypeScript = (props) => {
    var label: string = "Javascript";

    label = "TypeScript";

    return (
    <div>Hello {label}</div>
    );
}

export default TypeScript;